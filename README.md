# Food App Frontend

React + Vite frontend for the Food App project.

## Setup
1. `cd frontEnd`
2. Copy env template:
   - `frontEnd/.env.example` -> `frontEnd/.env`
3. Install dependencies:
   - `npm install`
4. Run dev server:
   - `npm run dev`

## Build
- `npm run build`
- `npm run preview`
